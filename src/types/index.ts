type VideoMetadata = {
  title: string;
  length: string;
  video_id: string;
  set_video_id: string;
  channel_url: string;
  thumbnail_url: string;
  categories: string[];
  topics: string[];
};

export type { VideoMetadata };
